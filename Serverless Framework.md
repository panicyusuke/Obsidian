#AWS 

# Serveless Framework

`AWS` のリソースをローカル環境でテスト、ビルド、デプロイするのは面倒だし時間がかかる。
`Serveless Framework` は、AWSの環境をローカルで再現することができるとっても素敵な`node` ベースのライブラリ。

`Lambda` のようなサーバーレスアーキテクチャはエンドポイント、機能ごとにソースコードを分離する必要がある。
こんなもの一々ディレクトリに分けて管理するのは死んでしまうのでServeless Frameworkと呼ばれるFaaS, サーバーレスのリソースを管理する仕組みが考えられた。

## デプロイ

- [[deploy]]
 
## ローカル環境での実行

- [[Offline]]

## ローカルでのテスト

T.B.D

## 別リソースをServerless Framework にインポートする

- [[Local Dynamo DB]]
